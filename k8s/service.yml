apiVersion: v1
kind: Service
metadata:
  name: gyhn
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "gyhn"
    port: 8080
  selector:
    app: gyhn
